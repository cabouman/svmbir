# Dependabot version updates: one grouped PR per ecosystem per week.
# GitHub reads this file from the default branch (master) only.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    target-branch: "prerelease"
    schedule:
      interval: "weekly"
    groups:
      github-actions:
        patterns: ["*"]

  - package-ecosystem: "pip"
    directories:
      - "/"
      - "/docs"
      - "/demo"
    target-branch: "prerelease"
    schedule:
      interval: "weekly"
    groups:
      pip:
        patterns: ["*"]
